请先进入计划模式，不要修改代码。

任务：
我想为食品科普网站建立统一内容模型，支持文章、谣言卡片、标签识读案例和权威来源条目。

请先分析：

1. 需要哪些类型。
2. 每种类型需要哪些字段。
3. 字段之间如何关联。
4. 哪些字段第一版必须有，哪些可以后续扩展。
5. 需要新增或修改哪些文件。
6. 是否会影响现有页面。

在我确认之前，不要改代码。


确认后：

按这个计划执行。
本次只允许新增或修改 types/content.ts 和必要的导出文件。
不要修改页面。
不要添加依赖。
完成后运行类型检查和 build。
